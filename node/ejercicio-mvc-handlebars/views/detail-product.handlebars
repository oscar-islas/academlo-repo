<div class="col-lg-12">

    <div class="card mt-4">
        <img class="card-img-top img-fluid" src='{{imgUrl}}' alt="">
        <div class="card-body">
        <h3 class="card-title">{{name}}</h3>
        <h4>{{price}}</h4>
        <p class="card-text">{{description}}</p>
    </div>
    <!-- /.card -->
</div>